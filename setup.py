from setuptools import setup, find_packages

setup(
    name='lvpocket',
    version='0.0.1',
    description='Detecting binding pockets in protein structures',
    url='https://github.com/ZRF-ZRF/LVpocket.git',
    packages=find_packages(include=["lvpocket"]),
    install_requires=[],
)
